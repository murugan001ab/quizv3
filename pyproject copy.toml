[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "sqlalchemy",
    "alembic",
    "python-jose[cryptography]",
    "bcrypt==4.0.1",
    "python-multipart",
    "pydantic[email]",
    "asyncpg",
    "websockets",
    "python-dotenv"
]